import { createStyles } from "../../styles/emotion-styles";

const styles = createStyles({
  inputContainer: {
    margin: "16px",
  },
  input: {
    marginRight: "8px",
  },
  showCompletedButton: {
    marginTop: "16px",
  },
});

export default styles;
